{
  "name": "rose-lab-site",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check:content && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "check:content": "node scripts/check-content.mjs",
    "validate": "npm run check:content && astro check",
    "new:article": "node scripts/new-article.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
